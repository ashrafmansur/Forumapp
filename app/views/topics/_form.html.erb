<h1><%= @topic.title %></h1>

<%= form_for @topic do |f| %>
   
   <%= f.label :title, "Title" %>
   <%= f.text_field :title %>

   <%= f.label :body, "Description" %>
   <%= f.text_area :body %> 

   <%= f.submit "Create" %>
<% end %>
