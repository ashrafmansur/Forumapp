<h1>New Comment</h1>

	<%= form_for [@topic, @comment] do |f| %>
  
   <%= f.label :body, "Comment" %>
   <%= f.text_area :body %>

   <%= f.label :user_id, "Written By" %>
   <%= f.text_field :user_id %>

   <%= f.submit "Create" %>
<% end %>