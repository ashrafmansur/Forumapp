<h1>New Comment</h1>

	<%= form_for [@topic, @comment] do |f| %>
  
   <%= f.label :body, "Comment" %>
   <%= f.text_area :body %>

   <%= f.hidden_field :user_id, :value => current_user.id %>

   <%= f.submit "Create" %>
<% end %>